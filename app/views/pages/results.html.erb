<div class="container">
  <h2 class="title-result">Voici ta planche de surf et ta combi idéale !</h2>

  <div class="conditions-1">
    <% if params[:weight].present? && params[:size].present? %>
      <p>Ces planches sont parfaites pour un poid de <%= params[:weight] %>kg et une taille de <%= params[:size] %>cm.</p>
    <% end %>
    <% if params[:level].present? && params[:waves].present? %>
      <p>Avec un niveau <%= params[:level].downcase %> tu seras capable de surfer des vagues <%= params[:waves] %> sans problème !</p>
    <% end %>
    <% if params[:objective].present? %>
      <p>Cette planche a été sélectionnée pour que tu puisses atteindre ton objectif : <%= params[:objective].downcase %>.</p>
    <% end %>
  </div>

  <div class="board-container">
    <div class="boards">
      <% @boards.take(10).each do |board| %>
        <div class="board">
          <%= image_tag board.image, width: 200, height: 250, crop: :fill, class: "image" %>
          <div class="board-cara">
            <p><strong>Nom : </strong> <%= board.name %></p>
            <p><strong>Dimensions de la planche : </strong> <%= board.dimension_length %>ft/ <%= board.dimension_height %>/ <%= board.dimension_thickness %></p>
            <p class="last-element"><strong>Volume de la planche : </strong> <%= board.volume %></p>
            <a class="button-card" href="https://www.boardshop.co.uk/surfboards">Je choisi cette planche !</a>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="conditions-2">
    <% if params[:temperature].present? %>
      <p>Ta combi a été sélectionnée pour ton confort entre <%= params[:temperature] %> degrés.</p>    <% end %>
  </div>

  <div class="board-container">
    <div class="wetsuits">
      <% @wetsuits.take(10).each do |wetsuit| %>
        <div class="wetsuit">
          <%= image_tag wetsuit.image, width: 200, height: 250, crop: :fill, class: "image"%>
          <div class="board-cara">
            <p><strong>Nom : </strong><%= wetsuit.name %></p>
            <p><strong>Epaisseur de la combinaison : </strong> <%= wetsuit.thickness %></p>
            <p class="last-element"><strong>Emplacement du zip : </strong> <%= wetsuit.zip %></p>
            <a class="button-card" href="https://www.boardshop.co.uk/wetsuits">Je choisi cette combi !</a>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

<div class="footer-result">
  <%= image_tag 'logolilguy.png', class: "img-footer"%>
</div>

<%# <div class="pair-container"> %>
  <%# <div class="pairs"> %>
    <%# <% @pairs.each do |pair| %>
      <%# <div class="pair"> %>
        <%# <p><%= pair[:board].name %>
        <%# <%= pair[:wetsuit].name</p> %>
      <%# </div> %>
    <%# <% end %>
  <%# </div> %>
<%# </div> %>
